% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/portable-runtime.R
\name{resolve_portable_runtime}
\alias{resolve_portable_runtime}
\title{Resolve a verified portable R runtime}
\usage{
resolve_portable_runtime(
  r_version = NULL,
  platform = NULL,
  arch = NULL,
  registry = getOption("rpackit.runtime_registry", .rpackit_runtime_registry),
  cache_dir = NULL,
  offline = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{r_version}{Exact R major.minor.patch version, or \code{NULL} for the newest
verified version available for \code{platform} and \code{arch}.}

\item{platform}{Runtime platform. Defaults to the current platform.}

\item{arch}{Runtime architecture. Defaults to the current architecture.}

\item{registry}{HTTPS URL or local path to a portable-R schema-v1 registry.}

\item{cache_dir}{Cache directory. Defaults to rpackit's user cache.}

\item{offline}{Reuse a matching same-registry cache entry without reading
any registry or artifact.}

\item{quiet}{Suppress download and extraction progress.}
}
\value{
An \code{rpackit_portable_runtime} object. \code{path} is the extracted R home
accepted by \code{\link[=prepare_desktop]{prepare_desktop()}}. The object also records \code{r_version},
\code{platform}, \code{arch}, registry and metadata sources, artifact URL, SHA-256,
archive format, cache path, and whether the cache was reused.
}
\description{
\code{resolve_portable_runtime()} selects a \code{verified} runtime from the rpackit
portable-R registry, downloads its archive over HTTPS, verifies its SHA-256,
checks every archive path before extraction, and atomically publishes the
extracted runtime to a user cache. A valid cache entry is reused without
downloading the artifact again.
}
\details{
Automatic extraction currently accepts ZIP artifacts only. Tar archives can
encode link targets that base R cannot prove safe before extraction, so
\code{tar.gz} and \code{tar.zst} metadata fail closed until a link-aware extractor is
part of the runtime contract. ZIP entries are inspected with their types;
symbolic links and all types other than ordinary files and directories are
rejected before extraction.

\code{registry} may be an HTTPS \code{versions.json} URL or a local file. Local
registries may refer to local metadata and artifact files, which supports
air-gapped mirrors and deterministic tests. This local transport override
uses the schema-v1 field set but is not valid public-registry metadata.
HTTP, UNC shares, and HTTPS URLs containing credentials, query strings, or
fragments are rejected so distributable provenance cannot expose secrets.

With \code{offline = TRUE}, no registry or artifact is read. A cache entry must
match the requested registry source, platform, architecture, and optional R
version. Cache entries are structurally revalidated and links or path escapes
are rejected. The user cache contains executable code and must not be
writable by untrusted users.

rpackit accepts the portable-R schema's conventional desktop subset: one
top-level R home, \code{bin/Rscript.exe} on Windows or \code{bin/Rscript} elsewhere,
and a runtime-local \code{library} directory.
}
\examples{
\dontrun{
runtime <- resolve_portable_runtime()
runtime$path
}
}
