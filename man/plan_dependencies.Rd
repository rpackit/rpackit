% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dependencies.R
\name{plan_dependencies}
\alias{plan_dependencies}
\title{Plan R application dependencies without executing application code}
\usage{
plan_dependencies(app_dir, include_suggests = FALSE)
}
\arguments{
\item{app_dir}{Path to the R application directory.}

\item{include_suggests}{Include \code{Suggests} and \code{Enhances} entries from
\code{DESCRIPTION}.}
}
\value{
An \code{rpackit_dependency_plan} object with \code{dependencies},
\code{references}, \code{diagnostics}, source-file paths, and R version requirements.
}
\description{
\code{plan_dependencies()} combines three sources of dependency information:
}
\details{
\enumerate{
\item \code{renv.lock} has highest precedence for exact package versions, sources,
and repositories.
\item \code{DESCRIPTION} supplies direct dependency roles and version constraints.
\item Parsed R calls discover packages that are used but not declared.
}

The sources are complementary rather than mutually exclusive. The
\code{references} table retains every observation, while the \code{dependencies} table
contains one resolved row per package. R source is parsed with \code{\link[=parse]{parse()}};
comments and string contents are never treated as package calls, and
application code is never evaluated.

In \code{dependencies}, \code{version}, \code{lock_source}, \code{repository}, and \code{remote} come
from \code{renv.lock}; \code{constraint} and \code{roles} come from \code{DESCRIPTION};
\code{constraint_satisfied} reports whether a locked version satisfies every
declared constraint; \code{direct} marks packages seen in source or DESCRIPTION;
and \code{locked} marks packages present in the lockfile. \code{provenance} is a compact
summary. The normalized \code{references} table is the authoritative record of
each origin, file, source line, role or call type, and version requirement.
Findings such as a dynamic \code{library()} package name appear in \code{diagnostics}.
Error diagnostics identify unsafe installation plans, including required
packages missing from a lockfile, locked versions that violate DESCRIPTION,
and DESCRIPTION \code{Remotes} without an exact \code{renv.lock}.
\code{has_description_remotes} and \code{description_remotes_count} expose only the
presence and count of remote specifications, not their possibly
credential-bearing text. Credential-bearing URL components in lockfile
remote provenance are redacted before they enter the returned tables.

Required DESCRIPTION fields (\code{Depends}, \code{Imports}, and \code{LinkingTo}) are
included by default. Set \code{include_suggests = TRUE} to also include \code{Suggests}
and \code{Enhances}. A lockfile package that is not directly referenced is retained
as a locked transitive dependency.
}
\examples{
app <- tempfile("rpackit-dependencies-")
dir.create(app)
writeLines(
  c("library(shiny)", "jsonlite::toJSON(list(ready = TRUE))"),
  file.path(app, "app.R")
)
plan_dependencies(app)
}
