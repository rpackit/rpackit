% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desktop-lifecycle.R
\name{desktop_app_launch_config}
\alias{desktop_app_launch_config}
\title{Return the authenticated native-shell launch contract}
\usage{
desktop_app_launch_config(process)
}
\arguments{
\item{process}{An \code{rpackit_desktop_process} returned by
\code{\link[=start_desktop_app]{start_desktop_app()}}.}
}
\value{
An \code{rpackit_desktop_launch_config} with \code{url}, exact \code{origin},
secret \code{headers}, covered \code{request_types}, and \code{follow_redirects = FALSE}.
}
\description{
Returns the secret-bearing request configuration for a running desktop app.
A native shell or local proxy must add \code{headers} to the initial navigation,
every subrequest, and every WebSocket upgrade for exactly \code{origin}. It must
not expose the header to browser JavaScript or forward it across a redirect.
}
\details{
This object contains the live session credential. Do not print its internal
fields, log it, serialize it, or persist it. Its print method deliberately
shows only non-secret metadata. New configurations become unavailable after
\code{\link[=stop_desktop_app]{stop_desktop_app()}} confirms cleanup, but any configuration already
returned is an ordinary R object whose credential cannot be revoked.
Consumers must drop every retained copy after the native handoff ends.
}
