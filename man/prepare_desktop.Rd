% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desktop-resources.R
\name{prepare_desktop}
\alias{prepare_desktop}
\title{Prepare portable desktop resources for an R application}
\usage{
prepare_desktop(
  app_dir,
  runtime_dir,
  output_dir = NULL,
  app_name = NULL,
  install_packages = TRUE,
  repos = getOption("repos"),
  verify_runtime = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{app_dir}{Path to a supported Shiny application.}

\item{runtime_dir}{Path to an extracted portable R home.}

\item{output_dir}{New output directory. Defaults to
\code{app_dir/dist/desktop-resources}.}

\item{app_name}{Human-readable application name.}

\item{install_packages}{Install required packages into the copied runtime.}

\item{repos}{Repository URLs used when installing packages.}

\item{verify_runtime}{Execute the supplied \code{Rscript --version} before
copying it.}

\item{quiet}{Suppress the completion summary.}
}
\value{
An \code{rpackit_desktop_bundle} object.
}
\description{
\code{prepare_desktop()} creates the versioned resource contract consumed by the
future rpackit Tauri shell:
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{output/
  resources/
    R/
    app/
    launcher.R
    rpackit.json
}\if{html}{\out{</div>}}

The application is inspected without execution. The supplied portable R
runtime is copied into the bundle, so the generated resources do not depend
on a system R installation at run time. By default, required packages are
installed into the copied runtime. A \code{renv.lock} uses \code{renv::restore()};
otherwise the parsed dependency plan is installed from \code{repos}.

The generated launcher accepts \code{--app}, \code{--port}, and \code{--token}, plus an
optional private \code{--control} path used for graceful shutdown. It binds Shiny
only to \verb{127.0.0.1}, exports the token to the application as
\code{RPACKIT_SESSION_TOKEN}, and emits versioned lifecycle events. Network-level
token enforcement belongs to the desktop shell/proxy and is deliberately
recorded as not yet implemented in \code{rpackit.json}; this function does not
claim to produce a Tauri executable.

Build inputs are never modified. Output is assembled in a sibling staging
directory and renamed into place only after validation. Existing output is
never overwritten.
}
